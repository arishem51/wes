# Base URL of the WES backend API. Leave as /api to use the Vite dev proxy.
VITE_API_BASE_URL=/api

# Set to "true" to run the UI against in-memory mock data (no backend required).
VITE_USE_MOCK=true
